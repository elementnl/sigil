# Changelog

## [Unreleased]

### Added
- Initial spec draft (SPEC.md)
- Document header section (`%%`, doctypes, metadata)
- Block type vocabulary (18 block types)
- Indentation rules (strict and lenient parser modes)
- Inline annotations (`{confidence:}`, `{asof:}`, `x-` custom keys)
- Typed references (`->` with relationship vocabulary)
